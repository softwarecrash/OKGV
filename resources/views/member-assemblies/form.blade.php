@extends('layouts.app')
@section('content')
<div class="container"><h1 class="h2">Mitgliederversammlung anlegen</h1><form method="POST" action="{{ route('member-assemblies.store') }}" class="card card-body">@csrf<x-validation-errors />
<label class="form-label">Titel<input class="form-control" name="title" maxlength="180" required value="{{ old('title') }}"></label><label class="form-label mt-3">Versammlungsform<select class="form-select" name="mode">@foreach($modes as $mode)<option value="{{ $mode->value }}">{{ $mode->label() }}</option>@endforeach</select></label><label class="form-label mt-3">Termin<input class="form-control" type="datetime-local" name="scheduled_at" required value="{{ old('scheduled_at',$assembly->scheduled_at->format('Y-m-d\TH:i')) }}"></label><label class="form-label mt-3">Ort oder Zugangshinweis<input class="form-control" name="location" maxlength="180"></label><label class="form-label mt-3">Elektronischer Rechteweg<textarea class="form-control" name="electronic_rights_notice" rows="3"></textarea></label><div class="form-text">Bei hybrid oder virtuell erforderlich: erkläre konkret Teilnahme, Rede-, Antrags-, Auskunfts- und Stimmrecht.</div><label class="form-label mt-3">Satzungsregelung / Mitgliederbeschluss für virtuell<textarea class="form-control" name="virtual_authorization_reference" rows="2"></textarea></label><div class="form-text">Bei rein virtueller Versammlung zwingend eintragen. OKGV prüft keine Rechtswirksamkeit.</div><label class="form-label mt-3">Tagesordnung<textarea class="form-control" name="agenda" rows="8" required>{{ old('agenda') }}</textarea></label><h2 class="h5 mt-4">Beschlussvorschläge</h2><p class="form-text">Diese Gegenstände werden mit der Einladung eingefroren. Mindestens ein Beschlussvorschlag ist erforderlich.</p>@for($i=0;$i<5;$i)<fieldset class="border rounded p-3 mb-2"><legend class="h6">Beschluss {{ $i+1 }}</legend><input class="form-control mb-2" name="resolutions[{{ $i }}][title]" placeholder="Titel" value="{{ old("resolutions.$i.title") }}" @if($i===0) required @endif><textarea class="form-control" name="resolutions[{{ $i }}][body]" placeholder="Beschlusswortlaut" @if($i===0) required @endif>{{ old("resolutions.$i.body") }}</textarea></fieldset>@endfor<button class="btn btn-primary align-self-start">Entwurf speichern</button></form></div>
@endsection
